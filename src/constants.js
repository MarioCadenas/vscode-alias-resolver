const ACTIONS = {
  CONFIGURE: 'Configure',
  CLOSE: 'Close',
};
const DEFAULT_FILE = 'index';
const DEFAULT_STYLES_FILE = 'style';

module.exports = {
  ACTIONS,
  DEFAULT_FILE,
  DEFAULT_STYLES_FILE,
};
